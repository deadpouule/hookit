import { DocsPage } from "@/components/docs/DocsPage";

export const metadata = {
  title: "Docs | hook it",
  description: "Launch, trade, hooks, fees, and contracts on Hookit.",
};

export default function DocsRoutePage() {
  return <DocsPage />;
}
