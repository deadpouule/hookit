import { BuilderPage } from "@/components/builder/BuilderPage";

export const metadata = {
  title: "Builder | hook it",
  description:
    "Compose MasterLaunchHook modules as a visual circuit, then launch a Uniswap v4 token on Hookit.",
};

export default function BuilderRoute() {
  return <BuilderPage />;
}
